# Monorepo Configuration
# This file contains root-level configuration for the monorepo
# App-specific configurations (Supabase keys, JWT secrets) should be in each app's .env file

# Environment
NODE_ENV=development

# PostgreSQL (for docker-compose.yml)
# Note: If using Supabase local (recommended), you don't need these
# Supabase provides its own PostgreSQL on port 54322
POSTGRES_HOST=localhost
POSTGRES_PORT=54322
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres

# Application Ports
NUXT_PORT=3001
API_PORT=3002
NITRO_PORT=3004
DOCS_PORT=3003
STORYBOOK_PORT=6006
VITE_PORT=3000

# API Base URL (for Nuxt frontend)
NUXT_PUBLIC_API_BASE=http://localhost:3002

# Logging
LOG_LEVEL=info

# Supabase Local Development
# Supabase runs from root ./supabase directory
# Start with: supabase start
# App-specific Supabase keys (SUPABASE_URL, SUPABASE_ANON_KEY, JWT_SECRET)
# should be configured in each app's .env file
# See apps/api/.env.example and apps/nitro/.env.example for templates
