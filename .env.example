# Monorepo Configuration
# This file contains root-level configuration for the monorepo
# Docker Compose reads environment variables from this file
# Apps can also use local .env files for development overrides

# Environment
NODE_ENV=development

# PostgreSQL (for docker-compose.yml)
# Note: If using Supabase local (recommended), you don't need to change these
# Supabase provides its own PostgreSQL on port 54322
POSTGRES_HOST=localhost
POSTGRES_PORT=54322
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres

# Database Connection String
# Replace <password> with your actual postgres password from `supabase status`
DATABASE_URL=postgresql://postgres:<password>@localhost:54322/postgres

# Supabase Configuration
# Supabase runs from root ./supabase directory
# Start with: supabase start
# Get credentials from: supabase status -o env
SUPABASE_URL=http://localhost:54321

# Option 1: New key system (2025+) - Recommended for new projects
# Uncomment and fill in from `supabase start` output
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=

# Option 2: Legacy JWT keys - Fallback for backward compatibility
# Get from `supabase status -o env`
# Only needed if not using new key system above
SUPABASE_ANON_KEY=
SUPABASE_AUTH_JWT_SECRET=

# CORS Configuration
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# Application Ports
NUXT_PORT=3001
API_PORT=3002
NITRO_PORT=3004
DOCS_PORT=3003
STORYBOOK_PORT=6006
VITE_PORT=3000

# API Base URL (for Nuxt frontend)
NUXT_PUBLIC_API_BASE=http://localhost:3002

# Logging
LOG_LEVEL=info
